# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# Virtual env
.venv/
venv/
env/
ENV/

# IDEs
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Testing / linting
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# Secrets — NUNCA committear
.env
.env.local
*.key
*.pem

# Datos grandes — el CSV original ya lo tenemos, NO lo incluyas en tu zip
# (sí puedes generar derivados en outputs/)
data/transactions_sample.csv

# Outputs generados (entrégalos, no los ignores en tu zip)
# outputs/  ← deliberadamente NO ignorado

# Delta Lake tables (parte1_pyspark.py) — binarios regenerables, no se versionan
outputs/delta/

# PySpark local run artifacts
derby.log
metastore_db/
spark-warehouse/